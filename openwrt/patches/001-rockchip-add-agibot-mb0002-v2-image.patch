diff --git a/target/linux/rockchip/image/armv8.mk b/target/linux/rockchip/image/armv8.mk
index 146af386..67de41b4 100644
--- a/target/linux/rockchip/image/armv8.mk
+++ b/target/linux/rockchip/image/armv8.mk
@@ -676,6 +676,20 @@ $(call Device/seewo_srcm3588-io)
 endef
 TARGET_DEVICES += seewo_srcm3588-sw
 
+define Device/agibot_mb0002-v2
+  DEVICE_VENDOR := AGIBOT
+  DEVICE_MODEL := MB0002 V2
+  SOC := rk3588
+  DEVICE_TYPE := basic
+  DEVICE_DTS := rk3588-agibot-mb0002-v2
+  UBOOT_DEVICE_NAME := agibot-rk3588
+  IMAGE/sysupgrade.img.gz := boot-common | boot-script vop | pine64-img | gzip | append-metadata
+  DEVICE_PACKAGES := kmod-drm-rockchip kmod-fb kmod-rknpu kmod-brcmfmac \
+	 ap6275p-firmware kmod-bluetooth kmod-hci-uart agibot-bt-attach \
+	 iw wpad-basic-mbedtls bluez-utils
+endef
+TARGET_DEVICES += agibot_mb0002-v2
+
 define Device/seewo_sv21
   DEVICE_VENDOR := Seewo
   DEVICE_MODEL := SV21

diff --git a/target/linux/rockchip/armv8/base-files/etc/board.d/02_network b/target/linux/rockchip/armv8/base-files/etc/board.d/02_network
index c4a13912..945b0eb1 100755
--- a/target/linux/rockchip/armv8/base-files/etc/board.d/02_network
+++ b/target/linux/rockchip/armv8/base-files/etc/board.d/02_network
@@ -7,6 +7,9 @@ rockchip_setup_interfaces()
 	local board="$1"
 
 	case "$board" in
+	agibot,mb0002-v2)
+		ucidef_set_interfaces_lan_wan 'eth0' 'eth1'
+		;;
 	advantech,rsb4810|\
 	ariaboard,photonicat|\
 	ariaboard,photonicat2|\
