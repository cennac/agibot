diff --git a/target/linux/rockchip/image/vop.bootscript b/target/linux/rockchip/image/vop.bootscript
--- a/target/linux/rockchip/image/vop.bootscript
+++ b/target/linux/rockchip/image/vop.bootscript
@@ -9,6 +9,6 @@ then serial_addr=',0xff1a0000';
 fi;
 
-setenv bootargs "console=ttyS2,1500000 console=tty1 earlycon=uart8250,mmio32${serial_addr} root=PARTUUID=${uuid} rw rootwait";
+setenv bootargs "console=ttyS2,1500000 console=tty1 root=PARTUUID=${uuid} rw rootwait";
 
 load mmc ${devnum}:1 ${fdt_addr_r} rockchip.dtb
 load mmc ${devnum}:1 ${kernel_addr_r} kernel.img
